# 更新日志

此项目的所有显著变更都将在此文件中记录。

格式基于 [Keep a Changelog](https://keepachangelog.com/en/1.0.0/)，
此项目遵循 [语义化版本](https://semver.org/spec/v2.0.0.html)。

## [未发布]

### 性能改进

- **降低CPU使用率**：将主循环从连续16ms重绘改为事件驱动渲染，仅在用户输入或计时器更新时重绘。
- **优化计时器处理**：将计时器tick率调整为1秒，仅在秒数变化时重绘，减少不必要的渲染。
- **常数时间操作**：添加了缓存计数器（`flags`、`opened_safe_cells`、`safe_cells`），使获胜条件检查和旗帜计数从O(n)降为O(1)。
- **内存优化**：使用静态字符串常量作为单元格内容，以减少渲染期间的堆分配。
- **渲染效率**：预分配具有已知容量的向量，以最小化重新分配。

### 技术细节

- 修改 `main.rs`：基于输入和计时器变化实现条件绘制。
- 更新 `model.rs`：添加性能计数器并优化 `update_timer`、`check_win`、`flags_count` 和 `recursive_open` 方法。
- 增强 `view.rs`：引入单元格字符串常量并改进向量分配。

### 修复

- 通过消除忙等待渲染循环解决了高CPU消耗问题。